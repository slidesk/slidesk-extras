# Shiki

More information on https://shiki.style

[WARNING] this plugin works only if you are online

You can change the theme using `SHIKI_THEME` variable in `.env`.

Example:

```
SHIKI_THEME=nord
```
